
export function addTeamsMembership(teamId, userId, substitutionId) {
  // Check that the team exists

  // Check if the user is already a member

  // Add the member of not already member

  // Set the database entry to active
}


export function removeTeamsMembership(teamId, userId, substitutionId) {
  // Check that the team exists

  // Verify that the user is a member

  // Check SDS if the user should permanently be a member

  // Remove membership if applicable

  // Update the database
}